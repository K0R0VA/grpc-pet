[package]
name = "proto-hub"
version = "0.1.0"
edition = "2024"

[dependencies]
prost = "0.14"
prost-types = "0.14"
tonic.workspace = true
tonic-prost = "0.14"

[build-dependencies]
tonic-prost-build = { version = "0.14", features = ["transport"] } # Крейт, который автоматически скомпилирует .proto файл